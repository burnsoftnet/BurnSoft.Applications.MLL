[`< Back`](./)

---

# FirearmCollection

Namespace: BurnSoft.Applications.MLL.Types

Class FirearmCollection list container for the local collection

```csharp
public class FirearmCollection
```

Inheritance [Object](https://docs.microsoft.com/en-us/dotnet/api/system.object) → [FirearmCollection](./burnsoft.applications.mll.types.firearmcollection)

## Properties

### **Id**

Gets or sets the identifier.

```csharp
public int Id { get; set; }
```

#### Property Value

[Int32](https://docs.microsoft.com/en-us/dotnet/api/system.int32)<br>
The identifier.

### **MyGunCollectionId**

Gets or sets my gun collection identifier. Actual field is MGCID

```csharp
public int MyGunCollectionId { get; set; }
```

#### Property Value

[Int32](https://docs.microsoft.com/en-us/dotnet/api/system.int32)<br>
My gun collection identifier.

### **FullName**

Gets or sets the full name.

```csharp
public string FullName { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The full name.

### **Manufacturer**

Gets or sets the manufacturer. Actual Field is Manu

```csharp
public string Manufacturer { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The manufacturer.

### **Model**

Gets or sets the model.

```csharp
public string Model { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The model.

### **Caliber**

Gets or sets the caliber. Actual field is Cal

```csharp
public string Caliber { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The caliber.

### **Barrel**

Gets or sets the barrel.

```csharp
public string Barrel { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The barrel.

### **SerialNo**

Gets or sets the serial no.

```csharp
public string SerialNo { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The serial no.

### **GunType**

Gets or sets the type of the gun. Actual Field is GType

```csharp
public string GunType { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The type of the gun.

### **Exclude**

Gets or sets a value indicating whether this [FirearmCollection](./burnsoft.applications.mll.types.firearmcollection) is exclude.

```csharp
public bool Exclude { get; set; }
```

#### Property Value

[Boolean](https://docs.microsoft.com/en-us/dotnet/api/system.boolean)<br>
`true` if exclude; otherwise, `false`.

### **LastSync**

Gets or sets the last synchronize for the sync_lastupdate column

```csharp
public string LastSync { get; set; }
```

#### Property Value

[String](https://docs.microsoft.com/en-us/dotnet/api/system.string)<br>
The last synchronize.

## Constructors

### **FirearmCollection()**

```csharp
public FirearmCollection()
```

---

[`< Back`](./)
